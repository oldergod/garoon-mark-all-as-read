// Closure does not seem up-to-date so we tell it we have those two.

/** @type {string} */
URL.prototype.pathname;

/** @type {string} */
URL.prototype.search;

/** @type {string} */
URL.prototype.hash;
